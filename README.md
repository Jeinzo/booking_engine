#Check or install

python --version node --version npm --version

#Install python dependencies pip install fastapi uvicorn pydantic pip install python-dotenv

#Start the backend server #go to backend/app directory

uvicorn main:app --reload

#Run React, open new terminal

cd frontend npm install npm run dev

#Open browser http://localhost:5173/
